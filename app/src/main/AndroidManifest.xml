<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rahbod.androidproject">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name="com.rahbod.androidproject.AppController"
        android:allowBackup="true"
        android:icon="@drawable/ic_mic_black_24dp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".IntroActivity"
            android:theme="@style/AppThemeNoAction" />
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/AppThemeNoAction" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppThemeNoAction">

        </activity>
        <activity
            android:name=".AddActivity"
            android:label="Add New Voice" />
        <activity
            android:name=".VoiceListActivity"
            android:label="Voices List" />
        <activity
            android:name=".CompareActivity"
            android:theme="@style/AppThemeNoAction" />
        <activity android:name=".TestVollyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>